package concurrency.printsequ;

import java.util.concurrent.atomic.AtomicInteger;

/*
    4个线程依次打印ABCDABCDABCD...
 */
public class PrintSequence {
    public static void main(String[] args) {
        /*AtomicInteger count = new AtomicInteger(0);
        int runCount = 4;
        ThreadPrint t1 = new ThreadPrint(count, "A",0, runCount);
        ThreadPrint t2 = new ThreadPrint(count, "B",1, runCount);
        ThreadPrint t3 = new ThreadPrint(count, "C",2, runCount);
        ThreadPrint t4 = new ThreadPrint(count, "D",3, runCount);
        t1.start();
        t2.start();
        t3.start();
        t4.start();*/

    }
}
