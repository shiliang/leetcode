package leetcode.DFSBFS;

import org.junit.Test;

import static org.junit.Assert.*;

public class PermutationTest {
    Permutation permutation = new Permutation();

    @Test
    public void getPermutation() {
        permutation.getPermutation(4, 15);
    }
}