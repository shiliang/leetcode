package leetcode.DFSBFS;

import org.junit.Test;

import static org.junit.Assert.*;

public class DecodeTest {

    @Test
    public void decodeString() {
        Decode decode = new Decode();
        decode.decodeString("3[a]2[bc]");
    }
}